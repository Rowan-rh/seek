# 排查报告 — {主题一句话}

> 排查会话: {session_id}（{chain_name} 链路）
> 排查时间: {start_time} ~ {end_time}
> 排查工具: seek CLI (deploy/sls/db/trace/ticket)
> 问题描述: {problem_description}

---

## 1. 快速结论

| 项 | 内容 |
|------|------|
| **结论类型** | 故障定位 / 咨询解答 / 巡检结论 / 证据不足待确认 |
| **归因类型** | DATA_SOURCE_PRIMARY / CODE_PRIMARY / DATA_CODE_INTERACTION / EXTERNAL_DEPENDENCY_PRIMARY / INSUFFICIENT_EVIDENCE |
| **归因置信度** | 高 / 中 / 低（说明判定依据） |
| **根因 / 归属** | {待填或{待确认}} |
| **影响范围** | {待填，不适用写"无"} |
| **紧急程度** | P0 / P1 / P2 / P3 / 无（巡检） |
| **建议操作** | {待填} |

---

## 2. 可复用排查路径与证据点

> 本节用于沉淀可复用的排查逻辑，供后续相似问题直接检索使用。只记录本次实际执行且有证据闭环的排查路径，不写未经验证的猜测或通用口号。

### 2.1 适用范围与前置条件

| 项 | 内容 |
|------|------|
| **适用问题特征** | {现象/错误码/API/模块关键词} |
| **复用搜索关键词** | {后续相似问题优先搜索的关键词，逗号分隔} |
| **前置输入** | {project、env、时间范围、traceId/uuid/任务ID 等} |
| **权限与依赖** | {需要的账号权限、SLS/DMS/Aone/外部平台依赖} |

### 2.2 标准排查路径

| 步骤 | 排查目标 | 命令/系统 | 输入条件 | 必采证据 | 判断与下一步 |
|------|----------|-----------|----------|----------|--------------|
| 1 | {确认问题入口/服务归属} | `{命令或系统}` | {输入} | {日志字段/返回字段/代码位置} | {证据满足时的判断；否则走哪条分支} |
| 2 | {确认部署/运行状态} | `{命令或系统}` | {输入} | {branch/commit/时间/环境} | {证据 → 判断 → 下一步} |
| 3 | {定位日志/调用/数据异常} | `{命令或系统}` | {输入} | {关键词、traceId、异常栈、SQL结果} | {证据 → 判断 → 下一步} |
| 4 | {选择同类正常样本并横向对比} | `{DB/SLS/回放/测试命令}` | {异常样本业务键、同环境/版本/业务类型} | {样本标识、可比性、字段差异、反证} | {数据/代码/共同作用/外部依赖/证据不足} |
| 5 | {验证外部依赖或修复结果} | `{命令或系统}` | {输入} | {状态码/回执/对照结果} | {证据 → 判断 → 结论} |

### 2.3 关键证据点

| 证据编号 | 来源命令/系统 | 查询条件与时间范围 | 关键字段/结果 | 支撑的判断 |
|----------|---------------|--------------------|---------------|------------|
| E1 | `{seek ... / Aone / SLS / DMS / 外部平台}` | `{条件}` | `{字段和值}` | `{支持哪个结论}` |
| E2 | `{来源}` | `{条件}` | `{字段和值}` | `{支持哪个结论}` |

> 适用时的固定证据铁律（按问题类型补充）：
> - **通知类**：`deliveryState` 是 Roar 返回的平台投递状态；运营商状态码从 `output` 字段提取（如 `output="fail:200005"`，`200005` 才是运营商回执码）。两者分别记录，不得混为同一证据。
> - **日志查询类**：关键词返 0 条时，必须记录 warnings/indexCheck 与 logstore 活性验证结果，禁止直接下"未执行/无数据"结论。
> - **跨 region 类**：单 region/单 logstore 扫描不得下"未接入"结论，必须写明验证边界。

### 2.4 分支决策与结论映射

| 证据条件 | 判断 | 后续动作/结论 | 证据编号 |
|----------|------|---------------|----------|
| {条件} | {判断} | {动作或根因结论} | {E1} |
| {条件} | {判断，包含验证边界} | {动作或根因结论} | {E2} |

### 2.5 复用边界

- **验证范围**：{region、环境、时间窗口、数据留存范围}
- **可直接复用**：{相同现象/错误码/模块/依赖条件}
- **需要重新验证**：{环境、版本、配置、region、外部回执等变化}
- **不可直接下结论的情况**：{权限不足、查询未执行、仅单 region/单 logstore、超出数据留存期等}
- **后续复用入口**：{后续相似问题先检索的关键词，以及建议先执行的步骤}

---

## 3. 排查过程

> 按本次实际执行的证据类型裁剪小节，未执行的整节删除，不得留空占位。

### 3.1 部署信息

| 项 | 值 |
|------|------|
| 目标项目 | {project} |
| 环境 | {env} |
| 部署分支 | {deployed_branch} |
| 近期变更 | {recent_changes}（是否有与问题时间吻合的部署） |

### 3.2 日志证据

**SLS 查询**: `seek sls logs/query {project} --env {env} ...`（时间窗: {time_window}）

| 时间 | 级别 | 消息摘要 |
|------|------|----------|
| {ts} | {level} | {message} |

**异常堆栈根因 / 关键时间线**: `{Caused by: ... 或状态变化时间线}`

### 3.3 DB / 配置证据

**SQL / 配置项**: `{sql 或配置路径}`

| 关键字段 | 值 |
|----------|------|
| {field} | {value} |

### 3.4 异常样本与正常样本对比

> 正常样本优先选择同环境、同部署版本、同业务类型且时间邻近的成功实例。无法取得可比样本或异常时刻快照时，必须说明查询范围并将最终归因降级为 `INSUFFICIENT_EVIDENCE`；已有直接复现或数据修正前后验证可作为等价对照。

| 对比项 | 异常样本 | 正常样本 | 差异及含义 | 证据编号 |
|--------|----------|----------|------------|----------|
| 样本坐标 | {业务主键/时间/环境/版本} | {业务主键/时间/环境/版本} | {可比性及不一致条件} | {E#} |
| 输入/DB源数据 | {值} | {值} | {是否违反业务约束} | {E#} |
| 配置/版本 | {值} | {值} | {是否为区分变量} | {E#} |
| 中间状态/执行路径 | {值} | {值} | {从哪一步开始分叉} | {E#} |
| 最终结果 | {失败表现} | {成功表现} | {支持或反驳何种归因} | {E#} |

**反证/替代解释**: {哪些事实可能推翻当前归因；尚未覆盖哪些变量}

### 3.5 调用链路 / 代码证据

**链路或代码位置**: {call_chain_analysis / code_location}

---

## 4. 样本对比与归因

- **归因类型**: {DATA_SOURCE_PRIMARY / CODE_PRIMARY / DATA_CODE_INTERACTION / EXTERNAL_DEPENDENCY_PRIMARY / INSUFFICIENT_EVIDENCE}
- **归因置信度**: {高/中/低及依据}
- **区分变量**: {异常样本与正常样本之间能解释结果差异的字段/条件}
- **反证检查**: {已排除项与仍可能推翻结论的证据}

---

## 5. 根因分析

- **直接报错点**: {最终在哪段代码/组件暴露异常，不等同于根因}
- **首要触发根因**: {最先使正常流程偏离的数据、配置、代码或外部条件；巡检/咨询类写"归属/结论"}
- **次生容错/放大问题**: {系统未正确拒绝、降级或解释异常输入的问题；无则写"无"}
- **因果链**: {首要触发条件 → 状态分叉 → 次生放大点 → 最终现象}
- **证据边界**: {region/时间窗/留存期/权限/外部依赖限制；每条结论对应的证据编号}

---

## 6. 修复建议

| 类型 | 建议 |
|------|------|
| **紧急止血** | {待填，无则写"无"} |
| **根本修复** | {待填} |
| **预防措施** | {待填} |

---

## 7. 未决事项

> 仅限因权限/外部依赖无法获取的信息。**不包含应在本次排查完成但遗漏的排查项**——遗漏项必须回头补查，不得放入未决事项。

| # | 待确认项 | 原因 | 建议获取方式 |
|---|----------|------|-------------|
| 1 | {待确认结论} | {权限不足/外部依赖/信息缺失} | {怎么获取} |

---

## 8. 更正记录

> 排查过程中发现前序结论有误时在此登记（对应 `seek chain amend` 回填）。无更正则写"无"。
> 更正必须写清三要素：**何时发现、因何更正、更正了什么**；相关结论/记忆同步修正并标注验证边界。

| # | 发现时间 | 原结论 | 更正后结论 | 更正依据（证据） | 影响范围 |
|---|----------|--------|-----------|-----------------|----------|
| 1 | {time} | {错误结论，含原验证边界} | {更正结论} | {日志/配置实测/SDK源码等证据} | {受影响的章节/记忆} |

---

> 所有结论必须有日志/DB/代码证据支撑，禁止猜测。不确定的结论标注 {待确认}。
