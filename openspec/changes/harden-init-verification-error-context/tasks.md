## 1. 初始化验证状态

- [x] 1.1 调整 `seek init` 的状态聚合，使 `ready` 继续表示本地配置就绪，`fully_ready` 只聚合 init 负责的 A1/DMS 在线验证和 SLS 本地配置检查。
- [x] 1.2 为 SLS 初始化结果补充清晰的验证边界与 doctor 引导，确保未执行 SLS 活性检查不会被表示为失败。
- [x] 1.3 保持默认 `seek init` 无外部验证调用，并处理 A1/DMS 验证失败时的结构化状态与 `next_actions`。

## 2. 错误上下文边界

- [x] 2.1 在错误日志模块集中定义最大嵌套深度、节点数量、UTF-8 总字节数和稳定截断标记。
- [x] 2.2 实现带预算状态的递归上下文脱敏，覆盖字典、列表、元组和标量，并在预算耗尽时停止遍历且保持合法 JSON。
- [x] 2.3 保留现有敏感键归一化规则、叶子值长度限制和日志写入失败不影响原始命令结果的行为。

## 3. 回归测试

- [x] 3.1 增加初始化测试：A1/DMS 验证成功且 SLS 凭据已配置时 `ready=true`、`fully_ready=true`，并验证 doctor 边界提示。
- [x] 3.2 增加初始化测试：未请求 verify 时不访问外部进程，SLS 未活性验证不导致 `fully_ready` 错误失败。
- [x] 3.3 增加错误日志测试：大列表、大字典和深层嵌套被截断，输出合法 JSON 且包含截断标记。
- [x] 3.4 增加错误日志测试：截断前后嵌套敏感键仍被掩码，标量类型和现有小型上下文结构保持兼容。

## 4. 验证与文档

- [x] 4.1 运行 CLI 全量单元测试并修复回归。
- [x] 4.2 运行 `openspec validate harden-init-verification-error-context --strict`。
- [x] 4.3 更新与 `seek init` 状态字段、SLS doctor 边界和错误日志截断行为相关的用户文档或命令能力说明。
